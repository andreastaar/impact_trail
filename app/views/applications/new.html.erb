<h2>esta seguro que desea aplicar a este voluntariado</h2>
<%= simple_form_for [@offer, @application] do |f| %>
  <div class="form-group">
  <%= f.input :status, :as => :hidden %>
  </div>
  <div class="form-group mt-4">
    <%= f.submit "Apply", class: 'btn btn-primary' %>
  </div>
<% end %>
<%= console %>
